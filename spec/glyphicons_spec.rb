require "spec_helper"

describe Glyphicons do
  it "has a version number" do
    expect(Glyphicons::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
